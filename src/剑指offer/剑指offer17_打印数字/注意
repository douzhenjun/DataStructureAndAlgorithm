打印从1到最大的n位数,这个最大的n位数其实可以很大,这就导致了如果采用将这个n位数单纯的用999...9这样的数
值来表示将会有很大的局限性.为了尽可能的表示较大的数,我们采用将这个最大的n位数作为一个字符数组而存在.
建立一个字符数组,它的长度为n.我们采用1.递归的方式,递归边界为此时的下标index到达了数组的末尾.每一次对这个n
字符数组的第index位按照顺序分别赋值0到9,因为是字符形式,所以采用int+char的方式,其中int的范围是0到9.每当
到达递归边界,就打印出一个数字来.
注意这里我们不能够打印出最高位为0的数字,所以我们需要判断第一个不为0的最高位,从这一位开始遍历打印.
比如打印三位数:
首先是000,001,...009,然后是010,011,...,099,最后是100,101,...999.